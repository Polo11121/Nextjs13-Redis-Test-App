import { CreateCarForm } from "@/createCar/components";

const CreateCarPage = () => (
  <div className="grid place-items-center h-screen">
    <CreateCarForm />
  </div>
);

export default CreateCarPage;
